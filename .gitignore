# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
backend/*.egg-info/
backend/dump.log*
backend/*.log

# Data (raw/processed datasets — regenerate via app.data.acquisition/cleaning, don't commit large files)
backend/data/raw/*.csv
backend/data/processed/*.csv

# Env / secrets
.env
backend/.env
frontend/.env
frontend/.env.local

# Node (frontend, later phase)
frontend/node_modules/

# OS
.DS_Store
Thumbs.db

# AI agent tooling / installed skill references (local machine state, not app code)
.agents/
.claude/
skills-lock.json
